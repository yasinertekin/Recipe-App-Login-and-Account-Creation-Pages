import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  input: {
    padding: 10,
    height: 40,
    margin: 12,
    borderRadius: 10,
    backgroundColor: '#BDC6CF',
  },
});
